name: Daily Game Review Video

on:
  schedule:
    - cron: "0 12 * * *"   # Runs daily at 12:00 UTC
  workflow_dispatch:       # Allows manual run

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Generate video with voice
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: python generate_video.py

      - name: Check for output.mp4
        run: |
          if [ ! -f output.mp4 ]; then
            echo "❌ output.mp4 not found! Generation failed."
            exit 1
          fi
          echo "✅ Video generated successfully."

      - name: Print refresh token length (debug - safe)
        # Map secret to an env var, then use bash to print only length.
        env:
          YOUTUBE_REFRESH_TOKEN: ${{ secrets.YOUTUBE_REFRESH_TOKEN }}
        run: |
          # Do NOT print the token itself. This prints only its length.
          echo "Refr
